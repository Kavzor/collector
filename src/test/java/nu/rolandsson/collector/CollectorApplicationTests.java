package nu.rolandsson.collector;

//@RunWith(SpringRunner.class)
//@SpringBootTest(classes = CollectorApplication.class)
public class CollectorApplicationTests {
 
 
//	@Test
	public void contextLoads() {
	//  Assertions.assertThat(5 < 10);
	  //mContext.registerBean(GeneralPurposeService.class, () -> new GeneralPurposeService());
	  //GeneralPurposeService service = mContext.getBean(GeneralPurposeService.class);
    //Assertions.assertThat(service.getRandomNumber(10) < 10);
	}
}
